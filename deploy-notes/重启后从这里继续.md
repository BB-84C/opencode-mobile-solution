# 重启后从这里继续

停机时间：2026-09-10 20:0x
本会话 ID：`ses_f796ca0bdffeNdehBrcTKLr8S2`

## 已完成且跨重启存活

**服务端全链路已部署并持久化**，三个 launchd 服务 `RunAtLoad=true`、`disabled=false`，**重启后自动拉起，无需人工干预**：

| 服务 | 端口 | 作用 |
|---|---|---|
| `com.skylerhu.opencode-backend` | 127.0.0.1:4096 | `opencode serve`，Basic 认证 |
| `com.skylerhu.opencode-relay` | 127.0.0.1:4097 | Bearer→Basic 翻译、passkey 控制台 |
| `com.skylerhu.opencode-caddy` | 100.125.254.14:8443 | TLS 终结，证书由本机 tailscaled 自动提供与续期 |

对外入口：`https://skylers-macbook-pro.taile806a6.ts.net:8443`

**配置与凭据**（`~/.config/opencode-relay/`，权限 600/700）：
`tokens.json`、`relay.env`、`backend.env`、`Caddyfile`、`bin/*.sh`

**relay 服务副本**：`~/.local/lib/opencode-relay-service/`，依赖完整，越权修复在位（不依赖源码目录，仓库移动不影响服务）

**源码分支**：`local/tailscale-relay-fixes`，改动未提交（`relay/lib/pairing-store.mjs` 越权修复、`app/app.json` bundle id）

**iOS 工程**：`app/ios/` 已 prebuild，`node_modules` 与 Pods 已装，App Intents 的 Swift 已进入 Sources 编译阶段

**文档**：`部署方案.md`、`给作者的审计报告.md`

## 重启后第一件事：验证服务自愈

这同时是 launchd 持久化的真实验收（此前只验证过手工重载，没验证过真重启）：

```bash
for p in 4096 4097 8443; do lsof -nP -iTCP:$p -sTCP:LISTEN >/dev/null 2>&1 && echo "OK $p" || echo "FAIL $p"; done
```

注意 Caddy 依赖 Tailscale 先起来（要绑 `100.125.254.14`）。若 Tailscale 启动较慢，Caddy 可能先失败，但 `KeepAlive=true` 会自动重试，等一两分钟再看。

若某个服务没起来：
```bash
launchctl print gui/$(id -u)/com.skylerhu.opencode-caddy | grep -E "state|last exit"
tail -20 ~/Documents/Files.ai/2026-09-09-opencode-mobile-ios/logs/com.skylerhu.opencode-caddy.err.log
```

## 被中断的任务

**iOS 平台支持包下载**，进度 2.9%（8.52 GB 中的约 250 MB），已干净停止，无残留缓存。需重新执行：

```bash
xcodebuild -downloadPlatform iOS
```

按当时速率（约 35 MB/分钟）需要约 4 小时。速度慢可能与 Shadowrocket 代理有关，重启后可先测一次裸速再决定要不要调整分流规则。

**没有这个包就无法编译到真机** —— 这是当前 iOS App 那条线唯一的硬阻塞。

## 两个待办

1. **注册 owner passkey**（需 Touch ID）。浏览器窗口会因重启丢失，重开命令（密钥不经过对话）：
   ```bash
   CFG=~/.config/opencode-relay
   open "$(grep '^RELAY_PUBLIC_ORIGIN=' $CFG/relay.env | cut -d= -f2-)/#setup=$(grep '^PASSKEY_BOOTSTRAP_TOKEN=' $CFG/relay.env | cut -d= -f2-)"
   ```
   注册后 `~/.config/opencode-relay/passkeys.json` 才会生成。

2. **iOS App 真机编译签名安装 + 扫码配对**。需要：平台包下载完成、Xcode 登录 Apple ID 选 Personal Team、iPhone 插线开启开发者模式。

## 环境事实（供快速回忆）

- Mac Tailscale：`100.125.254.14` / `skylers-macbook-pro.taile806a6.ts.net`
- iPhone：`100.93.254.63` / `iphone173`，蜂窝下为 IPv6 直连（41–118 ms），非中继
- Tailscale 与 Shadowrocket 已验证可共存；Shadowrocket 默认把 `100.64/10` 直连放行，Tailscale 为每个 peer 装 `/32` 路由压过它
- `~/.zshrc` 的 `NO_PROXY` 已加 `.ts.net` 与 `100.64.0.0/10`（两处：全局 export 与 `proxy()` 函数），备份在 `~/.zshrc.bak.20260910-183329`
- 磁盘曾降至 6.4 GB，现约 19 GB；CloudKit 缓存被部分清除后正在重新同步，空间可能短期波动
- 已知与本课题无关的问题：`explore` / `librarian` 子代理席位配在 `openai-hairen/gpt-5.6-luna`，该模型在网关账号池中不可用，派发即失败；`openai/gpt-5.6-luna`（OAuth 通道）可用，属系统配置层面的后续调整
